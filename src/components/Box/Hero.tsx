import React from 'react'
import Styles from '../../styles/Home.module.css'

const Hero = () => {
  return (
    <div className={Styles.hero}>
        <h2 style={{marginTop: "2rem", fontSize: "3rem"}}>Hero</h2>
    </div>
  )
}

export default Hero